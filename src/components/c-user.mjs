import request from '/lib/request.mjs';



window.customElements.define('c-user',
    class User extends HTMLElement {
        constructor() {
            super();
        }

        connectedCallback() {

        }

        disconnectedCallback() {
           
        }

        handleEvent(e) {
       
        }
    }
);